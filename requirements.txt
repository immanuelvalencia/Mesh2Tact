# Install Mesh2Tact and its development tools into the active Python environment.
# Activate your existing torch_gpu Conda environment first.
# Install a matching PyTorch/torchvision pair separately for train.py and Predict.
-e .[dev,train]
